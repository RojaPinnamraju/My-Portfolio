{
  "name": "portfolio-backend",
  "version": "1.0.0",
  "description": "Backend service for portfolio content extraction",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "build": "npm install && PUPPETEER_SKIP_CHROMIUM_DOWNLOAD=true npm install puppeteer-core@21.0.0",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "puppeteer-core": "^21.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
